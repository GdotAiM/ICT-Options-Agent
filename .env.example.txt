# Alpaca Paper Trading (REQUIRED for live trading)
ALPACA_ENDPOINT=https://paper-api.alpaca.markets/v2
ALPACA_API_KEY=PKDE5O5WBCNAABWKPYE5QOW723
ALPACA_SECRET_KEY=3PwcjYStWQRrqwCxsEAcVqtDDhV7LrXs5zkLsnrWiP3K
ALPACA_PAPER=true

# AI / LLM
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=your_key_here
AI_REQUIRED=true
AI_REASSESS_ENABLED=true
AI_REASSESS_MINUTES=15

# MCP / CLI execution path
USE_MCP=true
USE_ALPACA_CLI=false

# ── Risk Configuration ──
RISK_PCT=0.0075            # risk per trade as fraction of equity (0.75%)
MAX_POSITIONS=4            # maximum concurrent positions
MAX_CONTRACTS_PER_TRADE=10 # hard cap on contracts per order
MAX_PORTFOLIO_RISK_PCT=0.04 # max portfolio option risk (4% of equity)
MAX_PORTFOLIO_DELTA=50     # portfolio delta cap

# ── Kill Switch & Daily Loss ──
MAX_DAILY_LOSS_PCT=0.03    # halt new entries after 3% daily drawdown
FLATTEN_ON_KILL_SWITCH=true # auto-close positions when kill switch trips

# ── Exit Rules ──
ENABLE_EXITS=true
PROFIT_TARGET_PCT=0.50     # close at +50% unrealized P&L
STOP_LOSS_PCT=-0.50        # close at -50% unrealized P&L
MAX_DTE_TO_HOLD=2          # force close when DTE ≤ 2
EOD_FLATTEN=false          # flatten all positions at EOD time
EOD_FLATTEN_HOUR=15
EOD_FLATTEN_MINUTE=45

# ── Options Parameters ──
MIN_DTE=3                  # minimum days to expiration
MAX_DTE=14                 # maximum days to expiration
SPREAD_WIDTH_TARGET=5.0    # target spread width in dollars
MIN_OPEN_INTEREST=50       # minimum OI to consider a contract liquid

# ── Quote Quality Gates ──
MAX_QUOTE_SPREAD_PCT=0.25  # reject quote if spread > 25% of mid
MAX_QUOTE_AGE_SECONDS=30   # reject quote if older than 30s

# ── ICT Time Windows ──
REQUIRE_PRIMARY_WINDOW=false   # false = soft scoring; true = only primary windows
MIN_TIME_SCORE=0.35            # minimum time score to generate signal

# ── Underlying Universe ──
UNDERLYINGS=SPY,QQQ,IWM,AAPL,NVDA

# ── Autonomous Research Loop ──
RESEARCH_ENABLED=true
RESEARCH_MEMORY_LIMIT=12
RESEARCH_RESOLVE_ENABLED=true
